phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - cd frontend
      - npm install
  pre_build:
    commands:
      - npm run build
  post_build:
    commands:
      - aws s3 sync ./out s3://code-build-frontend --delete

artifacts:
  files:
    - '.next/**/*'
    - 'public/**/*'
    - 'package.json'

cache:
  paths:
    - 'node_modules/**/*'
    - '.next/cache/**/*'
